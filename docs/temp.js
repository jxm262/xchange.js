/**
 * Represents a book.
 * @param {string} title - The title of the book.
 * @param {string} author - The author of the book.
 * @returns - Thing
 */
var m = {asdf : " aadsfadsf"};

function book(title, author) {
    console.log('title');

}

module.exports = book;